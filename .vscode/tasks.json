{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/JWT.API/JWT.API.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}